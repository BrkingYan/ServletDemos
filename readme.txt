basic servlet learning demo
the projects dir should be placed in tomcat/webapps
and should change tomcat working dir to webapps/projects in tomcat/bin/server.xml

EmailLib dir is the necessary jars for Email servlet demo.

libs dir is some basic jars like mysql-connector.jar.

uploadLib dir has some jars for a file upload servlet demo.
